[tool.ruff]
target-version = "py310"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B"]
ignore = ["E501"]  # handled by line-length setting

[tool.ruff.lint.per-file-ignores]
"routes.py" = ["F401"]  # imports may be used for blueprints
